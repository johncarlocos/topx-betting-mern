# ============================================
# PRODUCTION ENVIRONMENT
# ============================================
# Copy this file to .env for production deployment
# cp .env.production.example .env
# 
# IMPORTANT: Update ALL values with your production credentials!
# DO NOT use default values in production!

# ============================================
# MongoDB Configuration
# ============================================
# MongoDB Atlas (Recommended for Production)
# Replace with your actual MongoDB Atlas connection string
ATLAS_URI=mongodb+srv://username:password@cluster.mongodb.net/betting-china?retryWrites=true&w=majority

# OR Self-hosted MongoDB (if using):
# ATLAS_URI=mongodb://username:password@your-mongodb-host:27017/betting-china?authSource=admin

# ============================================
# Redis Configuration
# ============================================
# Production Redis (Highly Recommended)
# Option 1: Redis Cloud / Managed Service
REDIS_HOST=your-redis-host.redis.cache.windows.net
REDIS_PORT=6380
REDIS_PASSWORD=your-very-secure-redis-password-here

# Option 2: Self-hosted Redis
# REDIS_HOST=localhost
# REDIS_PORT=6379
# REDIS_PASSWORD=your-very-secure-redis-password-here

# Option 3: Docker Redis (if using docker-compose)
# REDIS_HOST=redis
# REDIS_PORT=6379
# REDIS_PASSWORD=your-very-secure-redis-password-here

# ============================================
# Server Configuration
# ============================================
PORT=5000
NODE_ENV=production

# ============================================
# Security Configuration
# ============================================
# CRITICAL: Generate a strong random JWT secret for production!
# Generate using: openssl rand -base64 64
# Or: node -e "console.log(require('crypto').randomBytes(64).toString('base64'))"
# Minimum 32 characters, recommended 64+
JWT_SECRET=CHANGE-THIS-TO-A-VERY-SECURE-RANDOM-STRING-MINIMUM-64-CHARACTERS-LONG

# ============================================
# Feature Flags
# ============================================
# Enable automatic match data fetching in production
FETCHER=true
